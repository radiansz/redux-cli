import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class T extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

T.propTypes = propTypes;
export default T;
